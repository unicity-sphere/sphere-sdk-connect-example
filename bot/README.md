# Sphere Connect — Own-Wallet Bot Example

> Skeleton — filled in by a later task in the same plan
> (`.superpowers/sdd/task-b1-brief.md` / Task 4).

A Node bot that runs its **own** Sphere wallet (own keys, direct SDK usage) —
not a Connect dApp. See the plan doc for the full picture.
